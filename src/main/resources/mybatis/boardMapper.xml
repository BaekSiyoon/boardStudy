<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.example.board.board.mapper.boardMapper">

  <select id="list" resultType="boardVo">
    SELECT 
      seq, title, content, regId, regDate
    FROM 
      boardList
    ORDER BY 
      regDate DESC
  </select>

  <insert id="insertList"> 
    INSERT INTO 
      boardList(title ,content ,password ,regId, regDate)
    VALUES 
      (#{title}, #{content}, #{password}, #{regId}, NOW());
  </insert>

  <select id="detailList" resultType="boardVo">
    SELECT
      seq, title, content, regId, regDate
    FROM
      boardList
    WHERE
      seq = #{seq}
  </select>

  <select id="beforeList" resultType="boardVo">
    SELECT
      seq, title, content, regId, regDate
    FROM
      boardList
    WHERE
      seq = #{seq}
  </select>

  <update id="updateList" parameterType="hashmap">
    UPDATE 
      boardList
    SET 
      title = #{title}, content = #{content}, modId = #{modId}, modDate = NOW()
    WHERE 
      seq = #{seq}
  </update>

 </mapper>